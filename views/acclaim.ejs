<!-- start of section -->
<%- include('partials/_navbarAcclaim') %>
<section class="section-sellers-art">
  <% if(userId == 2) { %>}
  <!-- start of sellers art -->
  <div class="u-center-text u-margin-bottom-big">
    <h2 class="sellers-heading-secondary">Your Art</h2>
  </div>
  <div class="post-btn-container">
    <a href="/post" class="sellers-btn btn sellers-btn--white">Post your art</a>
  </div>
  <section class="section-sell-art">
    <!-- start of card -->
    <% for(let art of userArt) { %>
    <!-- start of card -->
    <article class="ai-card">
      <div class="ai-card__img-box">
        <img src="<%= art.photo %>" class="ai-card__img" alt="dhjdhdhjdjd" />
      </div>
      <div class="ai-card__desc-box">
        <p class="ai-card__p"><%= art.name %></p>
        <span class="artist"><%= art.user_name %></span>
        <div class="post-price">$<%= art.price %></div>
      </div>
      <div class="ai-card__sizes">
        <h4 class="ai-card-avail"></h4>
        <div class="poster"></div>
        <form action="/acclaim/<%= art.id%>" method="POST">
          <button
            class="ai-card-btn del-btn btn btn--white"
            type="submit"
            name="delete"
          >
            delete
          </button>
        </form>
      </div>
    </article>
    <% } %>
    <!-- end of card-->
  </section>
  <%}%>
  <!-- end of sellers art -->
  <div class="u-center-text u-margin-bottom-big">
    <h2 class="sellers-heading-secondary">our featured art</h2>
  </div>
  <!-- start of featured -->
  <section class="section-featured">
    <% for(let feature of featured) { %>
    <!-- start of card -->
    <article class="ai-card">
      <div class="ai-card__img-box">
        <img
          src="<%= feature.photo %>"
          class="ai-card__img"
          alt="dhjdhdhjdjd"
        />
      </div>
      <div class="ai-card__desc-box">
        <p class="ai-card__p"><%= feature.name %></p>
        <span class="artist"><%= feature.user_name %></span>
        <div class="post-price">$<%= feature.price %></div>
      </div>
      <div class="ai-card__sizes">
        <h4 class="ai-card-avail"></h4>
        <div class="poster"></div>
        <div class="ai-card__favorite" id="heart-<%= feature.id%>">
          <i
            class="fa-solid fa-heart <%= feature.is_favorite ? 'heart_red' : 'heart_grey' %>"
          ></i>
        </div>
      </div>
      <button class="ai-card-btn btn btn--white">
        <a href="/contact/<%= feature.email%>">Contact seller</a>
      </button>
    </article>
    <% } %>
    <!-- end of card-->
  </section>
  <!-- end of featured -->
  <div class="u-center-text u-margin-bottom-big">
    <h2 class="sellers-heading-secondary">Acclaim Artists</h2>
  </div>
  <!-- start of All -->
  <section class="section-all">
    <!-- start of card -->
    <% for(let card of cards) { %> <% if(card.seller_id != 2) { %>
    <!-- start of card -->
    <article class="ai-card">
      <div class="ai-card__img-box">
        <img src="<%= card.photo %>" class="ai-card__img" alt="dhjdhdhjdjd" />
      </div>
      <div class="ai-card__desc-box">
        <p class="ai-card__p"><%= card.name %></p>
        <span class="artist"><%= card.user_name %></span>
        <div class="post-price">$<%= card.price %></div>
      </div>
      <div class="ai-card__sizes">
        <h4 class="ai-card-avail"></h4>
        <div class="poster"></div>
        <div class="ai-card__favorite" id="heart-<%= card.id%>">
          <i
            class="fa-solid fa-heart <%= card.is_favorite ? 'heart_red' : 'heart_grey' %>"
          ></i>
        </div>
      </div>
      <button class="ai-card-btn btn btn--white">
        <a href="/contact/<%= card.email%>">Contact seller</a>
      </button>
    </article>
    <% } %> <% } %>
    <!-- end of card-->
  </section>
  <!-- end of of all -->

  <div class="u-center-text u-margin-bottom-huge"></div>
</section>
